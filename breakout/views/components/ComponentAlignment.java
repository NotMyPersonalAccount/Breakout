package breakout.views.components;

// ComponentAlignment contains enums for describing alignment of items in a container.
public class ComponentAlignment {
    // X axis.
    public enum X {
        LEFT,
        CENTER,
        RIGHT
    }
    // Y axis.
    public enum Y {
        TOP,
        CENTER,
        BOTTOM
    }
}
